---
'@astryxdesign/core': patch
---

[feat] DialogHeader: expose the trailing actions wrapper as a theme target

Adds `dialog-header-actions` so themes can style the existing end-content and close-button wrapper without relying on its DOM position. Default layout and behavior are unchanged.

@ksying
